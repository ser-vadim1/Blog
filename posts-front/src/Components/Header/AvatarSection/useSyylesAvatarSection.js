import { Typography } from "@material-ui/core";
import { makeStyles } from "@material-ui/core/styles";


export const useStylesAvatarSection = makeStyles((theme )=>({
      TypographyUserName: {
         paddingLeft: theme.spacing(2),
         fontFamily: "Akaya Telivigala, serif",
         fontSize:  "1.2em",
      },
  

}))